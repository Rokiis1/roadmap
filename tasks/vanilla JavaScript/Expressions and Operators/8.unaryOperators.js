// 1 Task.
/**
 * Increments a variable by 1 using the unary increment operator.
 * @param {number} variable - The variable to increment.
 * @returns {number} The incremented value of the variable.
 */

// 2 Task. 
/**
 * Checks if a number is even using the unary modulus operator.
 * @param {number} number - The number to check.
 * @returns {boolean} True if the number is even, otherwise false.
 */

// 3 Task.
/**
 * Converts a number to a negative value using the unary negation operator.
 * @param {number} number - The number to convert.
 * @returns {number} The negative value of the number.
 */
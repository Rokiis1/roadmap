// # Table of Contents

// - [Easy](#easy)
// - [Intermediate](#intermediate)
// - [Hard](#hard)

// # Easy

// 1 Task.
/**
 * Assign a value to a variable.
 * @param {number} value - The value to assign to the variable.
 * @returns {number} The assigned value.
 */
// Hints:
// Step 1: Declare a variable using let or const.
// Step 2: Use the assignment operator (=) to assign a value to the variable.
// Step 3: Return the assigned value.

// 2 Task.
/**
 * Add a number to a variable.
 * @param {number} base - The base number.
 * @param {number} number - The number to add.
 * @returns {number} The result of adding base and number.
 */
// Hints:
// Step 1: Declare a variable.
// Step 2: Use the addition assignment operator (+=) to add a number to the variable.
// Step 3: Return the result.

// 3 Task.
/**
 * Subtract a number from a variable.
 * @param {number} base - The base number.
 * @param {number} number - The number to subtract.
 * @returns {number} The result of subtracting number from base.
 */
// Hints:
// Step 1: Declare a variable.
// Step 2: Use the subtraction assignment operator (-=) to subtract a number from the variable.
// Step 3: Return the result.

// 4 Task.
/**
 * Multiply a variable by a number.
 * @param {number} base - The base number.
 * @param {number} factor - The number to multiply by.
 * @returns {number} The result of multiplying base by factor.
 */
// Hints:
// Step 1: Declare a variable.
// Step 2: Use the multiplication assignment operator (*=) to multiply the variable by a number.
// Step 3: Return the result.

// 5 Task.
/**
 * Divide a variable by a number.
 * @param {number} dividend - The number to be divided.
 * @param {number} divisor - The number to divide by.
 * @returns {number} The result of dividing dividend by divisor.
 */
// Hints:
// Step 1: Declare a variable.
// Step 2: Use the division assignment operator (/=) to divide the variable by a number.
// Step 3: Return the result.

// 6 Task.
/**
 * Calculate the remainder of a division.
 * @param {number} dividend - The number to be divided.
 * @param {number} divisor - The number to divide by.
 * @returns {number} The remainder of dividing dividend by divisor.
 */
// Hints:
// Step 1: Declare a variable.
// Step 2: Use the modulus assignment operator (%=) to calculate the remainder of a division.
// Step 3: Return the remainder.

// 7 Task.
/**
 * Concatenate two strings.
 * @param {string} str1 - The first string.
 * @param {string} str2 - The second string.
 * @returns {string} The concatenated string.
 */
// Hints:
// Step 1: Declare two variables as strings.
// Step 2: Use the concatenation assignment operator (+=) to concatenate the second string to the first.
// Step 3: Return the concatenated string.


// 8 Task.
/**
 * Append a string to another string.
 * @param {string} baseStr - The base string.
 * @param {string} appendStr - The string to append.
 * @returns {string} The result of appending appendStr to baseStr.
 */
// Hints:
// Step 1: Declare a variable as a string.
// Step 2: Use the addition assignment operator (+=) to append another string to it.
// Step 3: Return the updated string.

// 9 Task.
/**
 * Assign a boolean value to a variable.
 * @param {boolean} value - The boolean value to assign.
 * @returns {boolean} The assigned boolean value.
 */
// Hints:
// Step 1: Declare a variable.
// Step 2: Use the assignment operator (=) to assign a boolean value (true or false) to the variable.
// Step 3: Return the assigned boolean value.
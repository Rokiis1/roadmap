// # Table of Contents

// - [Easy](#easy)
// - [Intermediate](#intermediate)
// - [Hard](#hard)

// # Easy

// Task 1
/**
 * Compares two numbers and returns true if the first number is greater than the second number.
 * @param {number} num1 - The first number.
 * @param {number} num2 - The second number.
 * @returns {boolean} True if num1 is greater than num2, otherwise false.
*/
// Hints:
// Use the greater than (>) operator to compare num1 and num2.

// Task 2
/**
 * Checks if two strings are equal using the equality operator.
 * @param {string} str1 - The first string.
 * @param {string} str2 - The second string.
 * @returns {boolean} True if str1 is equal to str2, otherwise false.
 */
// Hints:
// Use the equality (===) operator to check if str1 is equal to str2.

// Task 3
/**
 * Determines whether a given number is divisible by 3 using the modulus operator.
 * @param {number} number - The number to check for divisibility.
 * @returns {boolean} True if the number is divisible by 3, otherwise false.
 */
// Hints:
// Use the modulus (%) operator to check if the number is divisible by 3.

// Task 4
/**
 * Checks if a given number is even.
 * @param {number} number - The number to check.
 * @returns {boolean} True if the number is even, otherwise false.
 */
// Hints:
// Use the modulus (%) operator to check if the number is divisible by 2.

// Task 5
/**
 * Checks if a string is empty.
 * @param {string} str - The string to check.
 * @returns {boolean} True if the string is empty, otherwise false.
 */
// Hints:
// Use the length property of the string to check if it has a length of 0.

// Task 6
/**
 * Determines whether a given number is positive (greater than zero).
 * @param {number} number - The number to check.
 * @returns {boolean} True if the number is positive, otherwise false.
 */
// Hnts:
// Use the greater than (>) operator to check if the number is greater than 0.

// Task 7
/**
 * Compares two boolean values and returns true if they are equal.
 * @param {boolean} bool1 - The first boolean.
 * @param {boolean} bool2 - The second boolean.
 * @returns {boolean} True if bool1 is equal to bool2, otherwise false.
 */
// Hints:
// Use the greater than (>) operator to check if the number is greater than 0.

// Task 8
/**
 * Compares two boolean values and returns true if they are equal.
 * @param {boolean} bool1 - The first boolean.
 * @param {boolean} bool2 - The second boolean.
 * @returns {boolean} True if bool1 is equal to bool2, otherwise false.
 */
// Hints:
// Use the equality (===) operator to compare bool1 and bool2.




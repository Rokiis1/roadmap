// 1 Task.
/**
 * Checks if a given number is between 10 and 20 (inclusive).
 * @param {number} number - The number to check.
 * @returns {boolean} True if the number is between 10 and 20 (inclusive), otherwise false.
 */
// Hint https://www.freecodecamp.org/news/javascript-capitalize-first-letter-of-word/

// 2 Task.
/**
 * Determines if a year is a leap year.
 * @param {number} year - The year to check.
 * @returns {boolean} True if the year is a leap year, otherwise false.
 */

// 3 Task.
/**
 * Determines if a user is eligible to vote based on their age and citizenship.
 * @param {number} age - The age of the user.
 * @param {boolean} isCitizen - Indicates if the user is a citizen (true) or not (false).
 * @returns {boolean} True if the user is eligible to vote, otherwise false.
 */

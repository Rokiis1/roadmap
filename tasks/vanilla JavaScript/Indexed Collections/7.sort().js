// 1 Task.
/**
 * Sorts an array of products by their price in ascending order.
 *
 * @param {Array<Object>} products - The array of product objects to sort.
 * @returns {Array<Object>} - The sorted array of product objects.
 */
// Hint: 
// Step 1: Use the sort method on the products array to perform the sorting.
// Step 2: Return the sorted products array.

// 2 Task.
/**
 * Sorts an array of students by their score in descending order.
 *
 * @param {Array<Object>} students - The array of student objects to sort.
 * @returns {Array<Object>} - The sorted array of student objects.
 */
// Hint:
// Step 1: Use the sort method on the students array to perform the sorting.
// Step 2: In the sorting callback function, compare the scores of students (b.score - a.score) to sort in descending order.
// Step 3: Return the sorted students array.
// Step 4: console.log().

// 3 Task.
/**
 * Sorts an array of usernames in ascending order.
 *
 * @param {Array<string>} usernames - The array of usernames to sort.
 * @returns {Array<string>} - The sorted array of usernames.
 */
// Hint:
// Step 1: Use the sort method on the usernames array to perform the sorting.
// Step 2: The sort method will sort the usernames in lexicographical order (ascending).
// Step 3: Return the sorted usernames array.
// Step 4: console.log().

// 1 Task.
/**
 * Calculates the total score from an array of scores using the reduce() method.
 *
 * @param {Array<number>} scores - The array of scores for each quarter.
 * @returns {number} - The total score.
 */
// Hint:
// Step 1: Use the reduce() method on the scores array to calculate the total score.
// Step 2: The reduce() method takes a callback function with two parameters: accumulator and currentValue.
// Step 3: In the callback function, accumulate the sum of scores by adding the currentValue to the accumulator.
// Step 4: Start with an initial value of 0 for the accumulator as the second argument to reduce().
// Step 5: Print the real-time data of scores for each quarter using console.log().
// Step 6: Call the calculateTotalScore function, passing the scores array as an argument.
// Step 7: console.log().

// 2 Task.
/**
 * Calculates the total cost of items in the shopping cart using the reduce() method.
 *
 * @param {Array<Object>} cartItems - The array of items in the shopping cart.
 * @returns {number} - The total cost of the shopping cart.
 */
// Step 1: Use the reduce() method on the cartItems array to calculate the total cost.
// Step 2: The reduce() method takes a callback function with two parameters: accumulator and item.
// Step 3: In the callback function, accumulate the total cost by adding the price * quantity of each item to the accumulator.
// Step 4: Start with an initial value of 0 for the accumulator as the second argument to reduce().
// Step 5: Print the real-time data of items in the shopping cart using console.log().
// Step 6: Call the calculateTotalCost function, passing the cartItems array as an argument.
// Step 7: console.log().


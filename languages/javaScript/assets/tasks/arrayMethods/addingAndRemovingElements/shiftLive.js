// 1 Task:
/**
 * Removes the first element from an array using the shift method.
 *
 * @param {Array} array - The input array.
 * @returns {*} The removed element.
 * @example shift([1, 2, 3]) // -> 1 and array is now [2, 3]
*/

// 2 Task:
/**
 * Creates an array and uses shift in a loop to empty the array.
 *
 * @returns {Array} The array after using shift in a loop to empty it.
 * @example createArray() // -> [] and array is now [] again after the loop empties it 
 */
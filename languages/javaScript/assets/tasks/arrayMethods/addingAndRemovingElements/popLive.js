// 1 Task:
/**
 * Removes the last element from an array using the pop method.
 *
 * @param {Array} array - The input array.
 * @returns {*} The removed element.
 * @example pop([1, 2, 3]) // -> 3 and array is now [1, 2]
 */

// 2 Task:
/**
 * Creates an array and repeatedly uses pop to remove elements until it is empty.
 *
 * @returns {Array} The array after repeatedly using pop.
 * @example createArray() // -> [] and array is now [] (empty)
 */

// 3 Task:

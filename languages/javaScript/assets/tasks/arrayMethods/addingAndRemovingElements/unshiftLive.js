// 1 Task:
/**
 * Adds an element to the beginning of an array using the unshift method.
 *
 * @param {Array} array - The input array.
 * @param {*} element - The element to be added to the beginning of the array.
 * @returns {number} The new length of the array after adding the element.
 * @example unshift([1, 2, 3], 4) // -> 4 and array is now [4, 1, 2, 3]
*/

// 2 Task:
/**
 * Creates an array and uses unshift to add multiple elements at the beginning.
 *
 * @returns {Array} The array with multiple elements added at the beginning.
 * @example createArray() // -> [1, 2, 3]
 */

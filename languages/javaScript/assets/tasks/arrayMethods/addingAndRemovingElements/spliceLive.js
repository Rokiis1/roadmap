// 1 Task:
/**
 * Replaces elements in an array using splice with elements from another array.
 *
 * @param {Array} arrayToUpdate - The array to update.
 * @param {number} start - The index at which to start changing the array.
 * @param {number} deleteCount - The number of elements to remove.
 * @param {Array} elementsToInsert - The elements to insert in place of the removed elements.
 * @returns {Array} The array after replacing elements.
 * @example splice([1, 2, 3, 4, 5], 1, 2, [6, 7]) // -> [1, 6, 7, 4, 5]
 */

// 2 Task:
/**
 * Removes elements from an array using splice and stores them in a separate array.
 *
 * @param {Array} arrayToRemoveFrom - The array to remove elements from.
 * @param {number} start - The index at which to start changing the array.
 * @param {number} deleteCount - The number of elements to remove.
 * @returns {Array} An array containing the removed elements.
 * @example splice([1, 2, 3, 4, 5], 1, 2) // -> [2, 3] and array is now [1, 4, 5]
 */
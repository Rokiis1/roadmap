// 1 Task:
/**
 * Adds an element to the end of an array using the push method.
 *
 * @param {Array} array - The input array.
 * @param {*} element - The element to be added to the array.
 * @returns {number} The new length of the array after adding the element.
 * @example push([1, 2, 3], 4) // -> 4 and array is now [1, 2, 3, 4]
 */

// 2 Task:
/**
 * Creates an empty array and uses push to add three elements to it.
 *
 * @returns {Array} The array with three elements added.
 * @example createArray() // -> [1, 2, 3]
 */

// 3 Task:
/**
 * Appends an array of numbers to another array of strings using push.
 *
 * @param {Array} stringArray - The array of strings.
 * @param {Array} numberArray - The array of numbers to append.
 * @returns {Array} The updated array with numbers appended.
 * @example appendArray(['a', 'b', 'c'], [1, 2, 3]) // -> ['a', 'b', 'c', 1, 2, 3]
 */


// 1. Task
/**
 * Doubles each element in the array.
 *
 * @param {number[]} arr - The input array.
 * @returns {number[]} - The array with each element doubled. example: [1, 2, 3] => [2, 4, 6]
*/

// 2. Task
/**
 * Capitalizes the first letter of each string in the array.
 *
 * @param {string[]} arr - The input array of strings.
 * @returns {string[]} - The array with the first letter of each string capitalized. example: ['hello', 'world'] => ['Hello', 'World']
*/

// 3. Task
/**
 * Applies a discount to each product's price in the array.
 * @param {Object[]} products - The array of product objects.
 * @param {number} discount - The discount percentage to apply.
 * @returns {Object[]} - The array with discounted prices. example: [{ price: 100 }, { price: 200 }] => [{ price: 90 }, { price: 180 }]
*/


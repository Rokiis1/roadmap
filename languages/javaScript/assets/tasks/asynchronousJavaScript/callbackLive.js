// 1. Task
/**
 * Executes a provided function after a specified delay.
 *
 * @param {number} delay - The time in milliseconds to wait before executing the callback.
 * @param {function} callback - The function to be executed after the delay.
 * @retun {void} The callback is executed after the delay. Example usage: delayedCallback(2000, logDelayedMessage);
 */

// 2. Task
/**
 * Fetches data from a specified URL and invokes a callback with the result.
 *
 * @param {string} url - The URL from which to fetch data.
 * @param {function} callback - The callback function to handle the fetched data.
 * @return {void} The callback is invoked with the fetched data. Simulated 2-second delay.
 *
 * @example
 * // Log the fetched data after 2 seconds
 * fetchData("https://api.example.com/data", handleFetchedData);
 *
 * @example
 * // Define a callback to handle the fetched data
 * function handleFetchedData(data) {
 *   console.log(data);
 * }
 */

/**
 * Handles the fetched data and logs a message to the console.
 *
 * @param {object} data - The fetched data.
 * @return {void} A message is logged to the console.
 *
 * @example
 * // Log the message from the fetched data
 * handleFetchedData(data);
 */

// 3. Task
/**
 * Attaches an event listener to a button element and invokes a callback
 * when the button is clicked.
 *
 * @param {string} buttonId - The ID of the button element.
 * @param {function} callback - The callback function to be executed on button click.
 * @return {void} The callback is invoked when the button is clicked.
 *
 * @example
 * // Attach an event listener to a button with ID "button" and log a message on click
 * eventListenerCallback("button", handleButtonClick); // Button clicked!
*/

/**
 * Handles the button click event and logs a message to the console.
 *
 * @return {void} A message is logged to the console.
 *
 * @example
 * // Log a message to the console
 * handleButtonClick(); // Button clicked!
*/



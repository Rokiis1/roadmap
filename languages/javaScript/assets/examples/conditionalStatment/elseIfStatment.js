/**
 * Assigns a grade to a student based on their score.
 * @param {number} score - The student's test score.
 */
if (score >= 90) {
    console.log('10: Excellent');
} else if (score >= 80) {
    console.log('8: Good');
} else if (score >= 70) {
    console.log('6: Satisfactory');
} else if (score >= 60) {
    console.log('5: Needs Improvement');
} else { 
    console.log('3: Fail');
}

/**
 * Determines the sign of a number and logs a message.
 * @param {number} num - The number to check.
 */

/**
 * Categorizes a temperature into "hot," "moderate," or "cold" based on a threshold.
 * @param {number} temperature - The temperature to categorize.
 * @param {number} threshold - The threshold for categorization.
 */

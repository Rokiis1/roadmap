/**
 * Logs a warning message if a user's age is below a certain threshold.
 * @param {number} age - The age of the user.
 * @param {number} threshold - The minimum age threshold.
 */

if (age < threshold) {
    console.log('Warning: User age is below the threshold.');
}

/**
 * Displays a message if a number is even.
 * @param {number} num - The number to check.
 */

/**
 * Notifies the user if a product is out of stock.
 * @param {boolean} isOutOfStock - Whether the product is out of stock.
 */



#include <stdio.h>

int main() {
    /*
    constants = fixed values that cannot be changed during the execution of a program.
    */

// good practice to use capital letters for constants
   const float PI = 3.14159;

    printf("PI = %.2f\n", PI); // PI = 3.14
    return 0;
}
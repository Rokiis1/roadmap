#include <stdio.h>
//  math.h = math header file (math library) (math functions) (mathematics) (maths) (math functions library) (math library functions) (math functions header file) (math functions library header file) (math library header file)
#include <math.h>

int main() {
    /*
    math functions = functions that perform mathematical operations (maths) (mathematics) (mathematical operations)
    */

// sqrt = Is a math function that returns the square root of a number
   double A = sqrt(9);
   printf("A = %f\n", A);
//    pow = Is a math function that returns the power of a number
    double B = pow(2, 3);
    printf("B = %f\n", B);
//    round = Is a math function that returns the rounded number of a number
    int C = round(2.3);
    printf("C = %d\n", C);
//    ceil = Is a math function that returns the rounded number of a number
    int D = ceil(2.3);
    printf("D = %d\n", D);
//    floor = Is a math function that returns the rounded number of a number
    int E = floor(2.3);
    printf("E = %d\n", E);
//    fabs = Is a math function that returns the absolute value of a number
    double F = fabs(-2.3);
    printf("F = %lf\n", F);
//    log = Is a math function that returns the natural logarithm of a number
    double G = log(2.3);
    printf("G = %f\n", G);
//    sin = Is a math function that returns the sine of a number
    double H = sin(2.3);
    printf("H = %lf\n", H);
//    cos = Is a math function that returns the cosine of a number
    double I = cos(2.3);
    printf("I = %lf\n", I);
//    tan = Is a math function that returns the tangent of a number 
    double J = tan(2.3);
    printf("J = %lf\n", J);

}
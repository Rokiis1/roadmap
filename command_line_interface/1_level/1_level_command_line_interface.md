# Content of Python command line level 1

The Python command line starts with the command `python` or `py` and is your gateway to interacting with the **Python interpreter** directly from your terminal or Command Prompt. You can also run **Python programs** by specifying a file name,`py script.py`. To check the installed Python version, you can use `python --version`.
